# M6 — 设置模块 子任务

> 职责：全部可配置参数管理 + 首次引导流程

---

## 数据层

- [ ] **6.1** 创建 `app/repositories/settings_repo.py`：`get(key)` / `set(key, value)` / `get_all()` / `batch_set(settings)`

## 服务层

- [ ] **6.2** 创建 `app/services/settings_service.py`：封装读写 + `DEFAULTS` 字典（14 个参数默认值）
- [ ] **6.3** 实现 `is_first_launch()` / `complete_onboarding()` — 首次启动判断

## UI 层 — 设置页

- [ ] **6.4** 创建 `app/screens/settings_screen.py`：表单式设置页（时间选择器、数字输入、工作日勾选）
- [ ] **6.5** 实现设置项：上午/下午上下班时间（4 个时间选择器）
- [ ] **6.6** 实现设置项：迟到/早退/旷工罚款金额 + 全勤奖励
- [ ] **6.7** 实现设置项：对赌基础奖励/超额奖励/惩罚金额
- [ ] **6.8** 实现设置项：工作日多选 + 拍摄日奖励 + 男友奖励门槛
- [ ] **6.9** 保存时发布 `SETTINGS_CHANGED` 事件

## UI 层 — 首次引导

- [ ] **6.10** 创建 `app/screens/onboarding.py`：8 步引导流程（Step 1→8 逐步完成）
- [ ] **6.11** 实现各步骤：上下班时间 → 工作日 → 罚款 → 全勤 → 男友门槛 → 拍摄日奖励 → 规划拍摄日(可选)

## 测试

- [ ] **6.12** 默认值读取 / 写入+回读 / 时间合法性校验 测试
- [ ] **6.13** 首次引导跳过可选步骤 + 完成后标记 测试
